const initialState = {
  characters: [],
  isFetchingData: false
};

export const characterReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
